version: '3.8'
services:
  api:
    build: .
    ports:
      - "4000:4000"
    env_file: .env
    depends_on:
      - mongo
      - redis
  mongo:
    image: mongo:6
    volumes:
      - mongo-data:/data/db
  redis:
    image: redis:7
  ml-service:
    build: ./python-ml-stub
    ports:
      - "5000:5000"
volumes:
  mongo-data:
